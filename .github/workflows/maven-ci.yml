
---

## 🚀 6. GitHub Actions CI – `.github/workflows/maven-ci.yml`

Create the folders `.github/workflows/` and add this file:

```yaml
name: Maven CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Build with Maven
        run: mvn -B clean verify
